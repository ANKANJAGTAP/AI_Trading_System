# ── Secrets / env (never commit broker or exchange credentials) ──
.env
.env.*
.secrets/
*.pem
*.key
*.token

# ── Local assistant / IDE state ──
.claude/
.idea/
.vscode/

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.ipynb_checkpoints/

# ── Data platform local data (never commit market data or DB files) ──
.aitrading_data/
*.parquet
*.duckdb
operational.db

# ── Data / logs ──
*.log
data/local/
dumps/

# ── Node (dashboard) ──
dashboard/node_modules/
dashboard/dist/

# ── OS ──
.DS_Store
