[pytest]
addopts = -q
testpaths = dataplatform/tests features/tests ml/tests fno_backtest/tests
# remove each test's tmp dir promptly so repeated runs don't accumulate
# large parquet/duckdb temp trees (which can choke shutdown cleanup).
tmp_path_retention_policy = none
filterwarnings =
    ignore::DeprecationWarning
